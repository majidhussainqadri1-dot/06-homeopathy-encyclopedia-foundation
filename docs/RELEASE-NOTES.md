# File 06 — Release Notes 2.4.6

File 06 v2.4.6 is the seventh fresh ten-round corrective repository candidate for the platform’s canonical homeopathy knowledge and research authority. It retains the governed File 06 v1.1 / Future Knowledge Intelligence 18 scope and applies the later ten-language publishing constitution where applicable.

Defects were found and corrected in rounds `1, 2, 3, 4, 5, 6, 7, 9, 10`; round `8` was clean.

Key corrections are: fail-closed research first-save publication mapping; side-effect-free public freshness reads; File 00-backed protected health authorization; governed taxonomy REST ownership; successful-case public rendering restricted to public, anonymized, consent-verified, non-retracted records; fail-closed non-public research title/excerpt/robots; publication-gated replacement and graph-linked public concept identifiers; and retry-safe freshness/research-gap maintenance cursors with persistence-failure propagation.

Runtime, contract, plugin metadata and aggregate QA are aligned to `2.4.6 / schema 10 / Future schema 2 / contract 2.4.6`.

The exact final automated run number, package SHA-256, package byte count and source-tree SHA-256 are deliberately not embedded in this source document. They must be taken from the completed exact-head final workflow so that source identity is not changed by recording its own digest.

This remains a repository/package candidate. Hostinger staging acceptance, exact deployed-build/DB/migration parity, real companion contract compatibility, browser/device/a11y/cache acceptance, backup/restore, rollback rehearsal, Founder acceptance and live verification remain separate external gates.
